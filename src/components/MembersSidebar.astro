---
interface NavItem {
  href: string;
  label: string;
}

interface Props {
  title?: string;
  subtitle?: string;
  currentPath?: string;
  topItems?: NavItem[];
  bottomItems?: NavItem[];
}

const {
  title = 'Mitgliederbereich',
  subtitle = 'Schön, dass du da bist.',
  currentPath = '',
  topItems = [],
  bottomItems = [],
} = Astro.props;

const isActive = (href: string) => currentPath === href || currentPath.startsWith(`${href}/`);
---

<aside class="self-start md:sticky md:top-8 bg-[#f3f1ec] p-5 md:p-6 space-y-6 w-full md:w-[240px] min-w-0">
  <div class="space-y-2">
    <p class="text-xs uppercase tracking-[0.18em] text-muted-foreground">{title}</p>
    <div class="bg-white/70 border border-border px-3 py-3">
      <p class="font-serif text-xl leading-tight break-words">{subtitle}</p>
    </div>
  </div>

  <nav class="space-y-1">
    {topItems.map((item) => (
      <a
        href={item.href}
        class={`block px-0 py-3 text-sm transition-colors border-b ${isActive(item.href) ? 'border-primary text-foreground font-medium' : 'border-border text-muted-foreground hover:text-foreground'}`}
      >
        {item.label}
      </a>
    ))}
  </nav>

  {bottomItems.length > 0 && (
    <div class="pt-4 mt-4 border-t border-border space-y-1">
      {bottomItems.map((item) => (
        <a
          href={item.href}
          class={`block px-0 py-3 text-sm transition-colors border-b ${isActive(item.href) ? 'border-primary text-foreground font-medium' : 'border-border text-muted-foreground hover:text-foreground'}`}
        >
          {item.label}
        </a>
      ))}
    </div>
  )}
</aside>
