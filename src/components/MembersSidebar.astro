---
interface NavItem {
  href: string;
  label: string;
}

interface Props {
  title?: string;
  subtitle?: string;
  currentPath?: string;
  topItems?: NavItem[];
  bottomItems?: NavItem[];
}

const {
  title = 'Mitgliederbereich',
  subtitle = 'Schön, dass du da bist.',
  currentPath = '',
  topItems = [],
  bottomItems = [],
} = Astro.props;

const isActive = (href: string) => currentPath === href || currentPath.startsWith(`${href}/`);
---

<aside class="self-start border border-border bg-background p-6 md:p-8 space-y-8">
  <div class="space-y-3">
    <p class="text-xs uppercase tracking-[0.18em] text-muted-foreground">{title}</p>
    <h2 class="font-serif text-3xl leading-none">{subtitle}</h2>
  </div>

  <nav class="space-y-1">
    {topItems.map((item) => (
      <a
        href={item.href}
        class={`block px-0 py-3 text-base transition-colors border-b ${isActive(item.href) ? 'border-primary text-foreground' : 'border-border text-muted-foreground hover:text-foreground'}`}
      >
        {item.label}
      </a>
    ))}
  </nav>

  {bottomItems.length > 0 && (
    <div class="pt-2 border-t border-border space-y-1">
      {bottomItems.map((item) => (
        <a
          href={item.href}
          class={`block px-0 py-3 text-base transition-colors border-b ${isActive(item.href) ? 'border-primary text-foreground' : 'border-border text-muted-foreground hover:text-foreground'}`}
        >
          {item.label}
        </a>
      ))}
    </div>
  )}
</aside>
