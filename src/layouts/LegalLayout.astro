---
import Layout from './Layout.astro'

interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---

<Layout title={title} description={description}>
  <section class="pt-28 md:pt-32 pb-16 md:pb-20">
    <div class="container mx-auto px-5 md:px-6">
      <div class="max-w-[700px] mx-auto prose prose-neutral">
        <slot />
      </div>
    </div>
  </section>
</Layout>

<style>
  .prose h1 { font-family: var(--font-serif); margin-bottom: 2.5rem; padding-bottom: 1.5rem; border-bottom: 1px solid hsl(0,0%,88%); }
  .prose h2 { font-family: var(--font-serif); margin-top: 3rem; margin-bottom: 1rem; font-size: 1.25rem; padding-top: 1.5rem; border-top: 1px solid hsl(0,0%,92%); }
  .prose h2:first-of-type { border-top: none; padding-top: 0; }
  .prose h3 { font-family: var(--font-serif); margin-top: 2rem; margin-bottom: 0.75rem; font-size: 1.125rem; }
  .prose p { margin-bottom: 1rem; line-height: 1.8; color: hsl(0,0%,35%); }
  .prose strong { color: hsl(0,0%,10%); }
  .prose a { color: hsl(107,57%,21%); text-decoration: underline; }
  .prose a:hover { opacity: 0.8; }
  .prose ul { list-style: disc; padding-left: 1.5rem; margin-bottom: 1.25rem; }
  .prose ul ul { margin-top: 0.5rem; margin-bottom: 0.5rem; }
  .prose li { margin-bottom: 0.5rem; line-height: 1.7; color: hsl(0,0%,35%); }
  .prose li:last-child { margin-bottom: 0; }
</style>
